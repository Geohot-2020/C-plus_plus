#include<iostream>
using namespace std;

int main()
{
	/*
	char ch = {};
	int Letters=0, Digital=0, Space=0, Other=0;
	while (ch = getchar() != '\n')
		if (ch >= 'A'&&ch <= 'Z' || ch >= 'a'&&ch <= 'z')//字母
			Letters++;
		else if (ch >= '0'&&ch <= '9')//数字
			Digital++;
		else if (ch == ' ')//空格
			Space++;
		else   //其他
			Other++;

	cout << "字母：" << Letters << "\n数字：" << Digital << "\n空格：" << Space << "\n其他：" << Other << endl;
	*/
	char ch = {};
	char ch1 = {};
	ch = (1 == 1);
	ch1 = (1 != 1);
	cout << ch<<endl;
	cout << ch1 << endl;
	}